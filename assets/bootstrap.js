import { Application } from '@hotwired/stimulus';

const app = Application.start();

// Ici tu peux enregistrer tes controllers Stimulus custom, par exemple :
// app.register('some_controller_name', SomeImportedController);

export default app;
